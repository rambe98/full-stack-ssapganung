리액트
자바 스크립트 라이브러리

노드js가 있어야 npm을 이용해 설치 가능
노드js의 경우 js코드를 브라우저 밖에서 실행할 수 있게 해주는 런타임 환경
js를 콘솔 or 터미널에서 확인하는 것이 가능해짐으로써 서버로 만드는게 가능해졌다.

npm을 이용해 리액트 프로젝트 생성하기

워크스페이스로 만들 폴더를 지정
터미널로 폴겨 경로 잡기

npm init 
패키지 이름, 버전정보, 패키지 설명등 여러 정보를 정해줘야함
package.json에 객체 상태로 저장된다.

스프링부트는 디펜던시스에서 버전만 적어놓고 적용하면 바로 바뀐다.

npm install react
npx를 이용한 리엑트 프로젝트 구성하기

워크스페이스로 사용할 폴더만들고
터미널로 폴더의 경로를 잘 잡는다


프론트엔드 서비스 개발
학습내용
컴포넌트
hook
props
이벤트 핸들러

실습 내용
app/todo/apptodo 컴포넌트 개발
material-ui를 이용한 ui개발
핸들러를 이용한 todo기능 개발


props와 hook
props : 부모 컴퍼넌트가 자식 컴포넌트에게 전달하는 값, 
props 는 객체이고 뭔가 넘겨 받으 ㄹ때 자삭 컴포넌트에서는 'props.이름' 으로 접근할 수 있다.

Hook
리액트 16.8에 새로 도입된 기능으로 함수형 컴포넌트에서 사용되는 몇가지 기술





