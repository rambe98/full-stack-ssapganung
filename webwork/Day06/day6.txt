메서드는 객체를 통해서만 호출이 가능하다.

자바스크립트에서 객체의 규격을 JSON이라고한다.

다양한 곳에서 데이터를 주고받을 때 JSON 규격을 활용한다.
특히 인터넷에서 데이터를 주고받을 때 모든 경우에 JSON이 활용된다고 생각해도 무방하다.

JSON에 데이터로 넣을 수 있는 타입
Number
String
Boolean
Array : 대괄호와 쉼표 사용
Object : 객체 안에 객체를 넣을 수 있음
null : 텅 빈 값으로 아무런 값도 없다라는 의미

Object 생성자
let person = new Object();

생성자 함수
function Person(name,gender){
	let married; >> private 취급함.
	this.name = name;
	this.gender = gender;
	this.sayHello = function(){
	}
}

let person1 = new person('홍길동','남');

함수
자주 사용하는 코드를 함수에 담아서 여러번 편하게 사용하기 위해 만드는 문법이다

함수의 호출
함수명(인자)

JS로 할 수 있는 것
HTML 요소를 만들거나, 변경하거나, CSS를 변경하는 것이 가능함

이벤트 처리
이번트 -> HTML 요소를 클릭, 글작성, 마우스 올리기 내리기 등등 어떠한 행위
이벤트 처리 -> 위 이벤트 발생 시 행동할 명령

인라인 이벤트 모델
<input type = "button" onclick = "기능">

표준 이벤트 모델
<input type = "button">

<script>
document.querySelector("input");
button.addEventListener(이벤트 종류,기능);
</script>


자주쓰는 이벤트들

이벤트		설명
click		요소에 마우스 클릭했을 때 이벤트가 발생
dbclick		요소에 마우스를 더블클릭했을 때 이벤트가 발생
mouseover	요소에 마우스를 오버했을 때 이벤트가 발생
mouseout	요소에 마우스를 아웃했을 때 이벤트가 발생
mousedown	요소에 마우스를 눌렀을 때 이벤트가 발생
mouseup		요소에 마우스를 떼었을 때 이벤트가 발생
mousemove	요소에 마우스를 움직였을 때 이벤트가 발생
contextmenu	context menu(마우스 오른쪽 버튼을 눌렀을 때 나오는 메뉴)가 나오기 번에 이벤트 발생

keydown		키를 눌렀을 때 이벤트가 발생
keyup		키를 떼었을 때 이벤트가 발생
keypress		키를 누른 상태에서 이벤트가 발생

focus		요소에 포커스가 이동되었을 때 이벤트 발생
blur		요소에 포커스가 벗어났을 때 이벤트 발생
change		요소에 값이 변경되었을 때 이벤트 발생
submit		submit 버튼을 눌렀을 때 이벤트 발생
reset		reset 버튼을 눌렀을 때 이벤트 발생
select		input이나 textarea 요소 안의 텍스트를 드래그하여 선택했을 때 이벤트 발생

load	페이지의 로딩이 완료되었을 때 이벤트 발생
abort	이미지의 로딩이 중단되었을 때 이벤트 발생
unload	페이지가 다른 곳으로 이동될 때 이벤트 발생
resize	요소의 사이즈가 변경되었을 때 이벤트 발생
scroll	스크롤바가 움직였을 때 이벤트 발생

웹 페이지 단축키 기능을 추가할 수 있음
e.key -> 내가 누른 버튼 정보가 들어있다.

마우스의 이동을 감지하는 이벤트

요소의 스타일 제어
요소의 스타일을 제어하려면 요소 객체를 대상으로 점을 찍고 style을 입력한다.
그리고 다시 점을 찍고 바꾸고 싶은 스타일을 작성한다.

요소.style.스타일속성 = '속성값';

미디어 쿼리로 충분히 변경을 할 수 있는 상황이면 굳이 js를 사용하지 않는것이 좋다.

js에서 스타일을 제어할 때는 스타일 속성을 카멜표기법으로 작성하는 것이 규칙이다.

요소의 속성 추출하기
속성은 영어로 attribute이다
js에서 속성을 열람할 때 getAttribute()함수를 사용한다.

요소.getAttribute('속성')

요소의 속성 제어하기
속성값 변경

자바스크립트는 html문서를 제어하기 위한 도구이므로. 요소의 속성을 열람하는 기능 뿐만아니라
조작하는 기능도 있다.

요소.setAttribute()

요소.setAttribute('속성','새로운 속성값');

이벤트를 통해 다른 요소의 속성을 제어하기

웹페이지 배포와 상용화
웹페이지를 인터넷에 연결하여 누구나 접속해볼 수 있는 상태로 만드는 행위를 배포라고한다

웹 페이지를 배포하는 절차
1. 웹페이지를 컴터에 저장한다
2. 컴터를 인터넷에 열견한다.
3. 컴터의 설정을 변경해 누구나 컴터에 접속할 수 있도록 허락한다
4. 외부인이 접속을 요총할 때마다, 컴터가 웹페이지를 보여준다

이때 사용된 컴터를 서버라고 부른다
사용자의 요청을 받아 서빙을 하는 도구라서 서버라고 부른다

웹 호스팅 서비스
서버 용도로 사용되는 컴터는 일반 컴터보다 비싸다
개인서버를 구매해서 사용하는 것은 수지타산에 안 맞다.
서버를 많이 구매하고 서버를 빌려주는 회사들을 웹 호스팅 회사라고 한다.
웹 호스팅 업체에서는 초기 비용을 조금만 투자하면 굉장히 다양한 사람들에게
서버를 빌려주면서 돈을 벌 수 있다.































